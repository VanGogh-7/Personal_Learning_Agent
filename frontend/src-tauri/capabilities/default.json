{
  "identifier": "default",
  "description": "Default desktop window capability with local file opening for registered library item paths.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-path"]
}
