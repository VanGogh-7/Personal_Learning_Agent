{
  "identifier": "default",
  "description": "Default desktop window capability with local file selection and opening for registered library item paths.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "opener:allow-open-path"]
}
