{
  "identifier": "default",
  "description": "Default desktop window capability with local file selection, note export, and opening for registered library item paths.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "dialog:allow-save", "opener:allow-open-path"]
}
